# Run ./scripts/setup.sh to create .env with generated passwords.
# Keep passwords as hex strings so they can be embedded safely in DATABASE_URL.
POSTGRES_PASSWORD=
APP_DB_PASSWORD=
FRONTEND_PORT=3002
